name: iptv_app
description: تجربة بث مباشر لقناة واحدة باستخدام Flutter و Better Player.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  better_player: ^0.0.83

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
